import { Box } from "@mui/material";
import React, { ReactElement } from "react";

export const HomeSlider = (): ReactElement => {
  return <Box sx={{ p: 5, width: "100%", boxSizing: "border-box" }}></Box>;
};
