import React, { ReactElement } from "react";
import { Box, Typography } from "@mui/material";

export const FeaturedBooks = (): ReactElement => {
  return <Box>
    <Typography variant="h4">Featured Books</Typography>

  </Box>
}
