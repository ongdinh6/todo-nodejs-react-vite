import React, { ReactElement } from "react";
import { Box } from "@mui/material";

const PageFooter = (): ReactElement => {
  return <Box>
    <p className={"text-center p-5"}>Page Footer</p>
  </Box>;
};

export default PageFooter;
